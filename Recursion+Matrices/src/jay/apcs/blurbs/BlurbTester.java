package jay.apcs.blurbs;

public class BlurbTester {
	public static boolean isBlurb(String blurb) {
		if (!blurb.startsWith("x"))
			return false;

		// TODO check if the string is a valid blurb
		return false;
	}
}
